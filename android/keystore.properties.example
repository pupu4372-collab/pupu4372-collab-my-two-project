# Copy to android/keystore.properties (never commit keystore.properties).
# Generate the keystore first: see scripts/create-android-upload-keystore.ps1

storeFile=ksajupet-upload.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=upload
keyPassword=YOUR_KEY_PASSWORD
